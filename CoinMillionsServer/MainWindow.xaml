<Window x:Class="CoinMillionsServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="750">
    <Grid>
        <RichTextBox x:Name="outputBox"
                     VerticalScrollBarVisibility="Visible" 
                     HorizontalScrollBarVisibility="Visible" 
                     IsReadOnly="True" Margin="0,180,0,0" Padding="0" FontFamily="Lucida Console" FontSize="10"/>
        <ProgressBar x:Name="progressBar1" Height="40" Margin="0,0,0,0" Padding="0" Width="700"/>
        <Label x:Name="lblTicks" Content="" HorizontalAlignment="Left" Margin="258,91,0,0" VerticalAlignment="Top"/>
    </Grid>
</Window>
